services:
  web:
    build:
      context: .
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: sqlite:////app/data/app.db
      FLASK_DEBUG: "0"
      PORT: "5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
